# 工作流：发布（简版）

## 发布前

- [ ] 主分支已合并待发布变更；CI（若有）通过。
- [ ] `.ai/context` 中与对外行为相关的条目已更新。
- [ ] 环境变量与密钥已在目标环境配置（不回填到仓库）。

## 部署（DevOps）

- 执行构建与部署流程；健康检查访问 `GET /api/health`。

## 发布后

- 短时观察日志与指标；重大变更准备回滚步骤。
