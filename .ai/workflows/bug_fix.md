# 工作流：缺陷修复

## 输入

- 复现步骤、期望 vs 实际、环境（分支、是否生产）。

## 步骤

1. **定位**：QA / Backend / Frontend Agent 根据边界缩小范围（日志、请求、数据）。
2. **修复**：在对应仓库 (`sensoube` 或 `senso_admin`) 最小改动修复。
3. **回归**：补充或更新自动化测试防止复发；`npm test`。
4. **Reviewer**：审查风险（尤其是安全与并发）。

## DoD

- [ ] 根因简述（可写在 PR 或 issue）
- [ ] 测试覆盖或注明无法覆盖原因
